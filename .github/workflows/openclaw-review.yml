name: OpenClaw PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    runs-on: self-hosted
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Chạy OpenClaw Review
        id: openclaw-step
        env:
          GITHUB_TOKEN: ${{ secrets.OPENCLAW_PAT }}
        run: |
          PR_URL="${{ github.event.pull_request.html_url }}"
          
          echo "Bắt đầu gọi openclaw..."
          OUTPUT=$(openclaw agent --message "Review PR này: $PR_URL")
          echo "Output nhận được là: $OUTPUT"
          
          # Ghi multiline output an toàn bằng EOF thẳng hàng
          echo "review_result<<EOF" >> $GITHUB_OUTPUT
          echo "$OUTPUT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Đăng comment kết quả lên PR
        uses: actions/github-script@v7
        env:
          # Đưa kết quả của OpenClaw vào biến môi trường an toàn
          REVIEW_OUTPUT: ${{ steps.openclaw-step.outputs.review_result }}
        with:
          github-token: ${{ secrets.OPENCLAW_PAT }}
          script: |
            // Lọc và đọc an toàn từ biến môi trường, không sợ lỗi ký tự đặc biệt
            const rawOutput = process.env.REVIEW_OUTPUT || "Không có nội dung trả về.";
            
            const reviewBody = `🤖 **OpenClaw PR Review:**\n\n${rawOutput}`;
            
            await github.rest.issues.createComment({
              issue_number: context.payload.pull_request.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: reviewBody
            });